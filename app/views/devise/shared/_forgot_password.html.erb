<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= link_to 'Forgot your password?', new_password_path(resource_name), class: 'text-purple-light hover:text-purple cursor-pointer antialiased h-auto' %><br />
<% end %>
